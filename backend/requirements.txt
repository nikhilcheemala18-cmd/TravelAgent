fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# Provider SDKs — only required when that provider is selected via
# LLM_PROVIDER; each is imported lazily by its own app/llm/providers/*.py
# file. The default LLM_PROVIDER=mock needs neither.
openai>=1.30
google-genai>=1.0
